# Django, FastAPI, or any other HTTP API endpoint
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001

# Frontend-only MVP. Move this key behind the backend proxy before production.
NEXT_PUBLIC_TMAP_APP_KEY=
